data:
  class_path: main.datamodule_diffusion.WebDatasetDatamodule
  init_args:
    train_dataset: main.dataset_diffusion.create_sfx_dataset
    train_dict_args:
      path: null
      # path: ${DIR_DATA}
      sample_rate: 48000
      chunk_size: 262144
      shift_augment: True
      cut_prefix: True
      one_chunk_per_track: False
      shardshuffle: True

    val_dataset: main.dataset_diffusion.create_sfx_dataset
    val_dict_args:
      path: null
      # path: data/DIFF-SFX-webdataset/greatest_hits/val_shard_1.tar
      sample_rate: 48000
      chunk_size: 262144
      shift_augment: False
      cut_prefix: True
      one_chunk_per_track: False
      shardshuffle: False
    
    batch_size: 4
    num_workers: 8
    pin_memory: True
    shuffle_size: 200
    collate_fn: main.dataset_diffusion.collate_fn